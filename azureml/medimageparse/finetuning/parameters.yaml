datamodule:
  dataloaders:
    train:
      batch_size: 2
    validate:
      batch_size: 2

optimizer:
  lr: 0.00002
  weight_decay: 0.01

trainer:
  max_epochs: 10
  devices: 1 # number of GPUs per node
  num_nodes: 1 # number of nodes in the cluster